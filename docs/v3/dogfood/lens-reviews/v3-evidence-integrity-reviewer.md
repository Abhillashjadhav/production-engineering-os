# PD-V3-15 — Lens 6 (Eval & Evidence Integrity) — CAND-001

All inspection was read-only; nothing was fixed, re-run, or regenerated.

## Candidate digest (verified first)

- **CAND-001**, commit `243eddf72005f6f23ed70142053adbd27f7ae3c3`, tree digest `sha256:3af7832792afa78c12aa88efab6030ebfeafdd45eb75f45c3d9a943470c03bcb`, contract digest `sha256:f01af9c278d32e5c3c953f151662958c8580f109938e44016ac802dc563bad6f`.
- Cross-checked identical across `candidate-manifest.json`, `candidates/CAND-001.json`, the freeze event (ledger line 19), and the browser/preview event input digests. Repo HEAD (`.git/HEAD` → `refs/heads/v3/pr16-dogfood`) is at exactly `243eddf72005…`. Caveat: this lens has no execution tool (Read/Grep/Glob only), so the tree digest was verified by cross-artifact consistency, not recomputed.

## Findings

| ID | Sev | Pointer | Defect | What it could hide |
|---|---|---|---|---|
| F1 | MEDIUM | run `ledger.jsonl` lines 6–17 | All 12 implement events (task_tests/task_implementation for T-PR04..T-PR09) were written in 0.8 ms (12:03:12.740713–.741470) — a replay of pre-merged history; disclosure lives only in `scripts/dogfood_v3.py:52-61` ("mirrors the merged history"), not in the ledger events themselves | A ledger consumer treating the replayed tests-before-implementation ordering as executed red-first proof for *this* run |
| F2 | MEDIUM | ledger line 20; `products/pm-evals-web/e2e/playwright.config.ts:20`; `.../e2e/test-results/.last-run.json` | Browser claim "suites=a11y,keyboard,responsive,journeys;mocked=false, 14 tests passed" has no retained per-test artifact: list reporter + trace retain-on-failure keep nothing on green; the sole survivor `.last-run.json` (`{"status":"passed"}`) is gitignored, untimestamped, and unbound to CAND-001 (could pre-date the run — PR 28/29 reviewers re-ran the suite) | A stale or partial suite run being claimed as this candidate's browser verification |
| F3 | MEDIUM | `playwright.config.ts:58`; `scripts/dogfood_v3.py:141`; `src/pmpe/assurance/readonly_guard.py:19-21` | `next start` serves whatever `.next` build pre-exists; `.next` is gitignored and excluded from every tree digest; BUILD_ID `zgaTNnW423y8RYYju83oH` is recorded but nothing binds it to commit 243eddf | Browser + preview evidence executed against a stale frontend build under the frozen candidate's name |
| F4 | MEDIUM | `src/pmpe/fullstack/orchestration.py:270`; run `preview-evidence.json:14` | `output_digests.preview_evidence` is the caller-supplied *source* digest, not a hash of the evidence file; the file's `source_digest` `sha256:6db7fc…` is a products-subtree digest (`dogfood_v3.py:68-87`) ≠ candidate tree digest `sha256:3af783…`, re-derivable only by re-running `_source_digest` at the frozen commit (not re-derivable in this audit) | Post-hoc edits to `journeys`/`artifacts` inside preview-evidence.json invisible to the ledger |
| F5 | LOW | `orchestration.py:150`; `src/pmpe/fullstack/journey.py:122-127` | `journey_record` digest is the contract digest by construction; `require_validated_journey` checks only the embedded `contract_digest` field of `ux-architecture.json` — the journey/screen inventory itself is not content-bound anywhere | Post-validation tampering with the journey inventory that every later gate still accepts |
| F6 | LOW | ledger (freeze at line 19, zero review/readonly_check events at audit time); `src/pmpe/evals/trajectory_fullstack.py:169-186` | `evaluate_fullstack_trajectory` over the audit-time ledger returns **TRAJ-FS-05** (all six lenses missing after a freeze). Expected mid-run per `lens-reviews/00-recovery-note.md`, but the run is releasable only after six `submit_review` + `clean` `readonly_check` events land — as-is, `finish` is a hard HOLD | A release report issued on the audit-time ledger would ship an unreviewed frozen candidate |
| F7 | LOW | ledger lines 5, 11, 13, 15, 17 | Routing selected no frontend specialist, and all four frontend tasks (T-PR06..T-PR09) record `v2-backend-engineer` as implementer — a misattribution in the replayed evidence relative to the documented PR history | Evidence-record inaccuracy: agent attribution in the ledger cannot be trusted as provenance |
| F8 | LOW | `docs/CONTRIBUTION_LEDGER.md` rows 25–33 (lines 37–45) | Red-first ("tests committed red before implementation, branch history") and named mutation-kill claims are documented per PR with independent reviewer verification, but commit-order re-derivation was not possible in this audit (no git/exec tool); row 33 — the FullStackRun orchestration that emitted this very ledger — still shows reviewer verdict and merge commit "pending" | The evidence-producing code's own review trail is unclosed at the time its output is being trusted |

## Verdicts per evidence class

- **Ledger grammar** — **PASS**. Events conform to the TRAJ-FS grammar (`trajectory_fullstack.py`): FS-01 (journey at line 2 precedes first frontend implement at line 10), FS-02 (journey input == locked contract `f01af9…`), FS-03 (`mocked=false` declared), FS-04 (preview `kind=local_preview`, input == freeze output), FS-08 (`api_contract`/`current` at line 18, before freeze). FS-05/06 open — see F6.
- **Candidate binding** — **PASS** (by cross-artifact consistency + HEAD match; tree-digest recomputation not performed with available tools).
- **Preview binding** — **NOT_PROVEN**. Ledger-level binding to the frozen candidate holds, but the artifact's subtree source digest (`6db7fc…`) is not independently re-derivable here and the evidence file itself is not content-hashed into the ledger (F3, F4).
- **Browser evidence** — **PASS** with caveats. All four claimed suites genuinely exist (`e2e/tests/a11y.spec.ts`, `keyboard.spec.ts`, `responsive.spec.ts`, `journey.spec.ts`), the harness launches a real uvicorn backend and real production `next start` frontend, and a repo-wide grep found zero `page.route`/interception/stubbing in the delivered path; assertions are falsifiable (axe full-violation equality, engine-true numbers `+9.4%`/`93.8%`/`T-006`, bounded tab-order with computed-style focus check). Execution is corroborated by the 22 s freeze→verification window and `.last-run.json`, but per-test artifacts are not retained or candidate-bound (F2, F3).
- **Test meaningfulness** — **PASS**. Sampled backend tests reach the behavior their names claim (`backend/tests/test_api.py`: byte-level OpenAPI pin :157, spool-residue no-persistence :145, hostile-filename header pin :102, real-sha256 digest pin :181); frontend vitest specs run the real typed client with only the network injected — disclosed in-file — and pin inverse-direction locks, object-URL revocation counts, and role/aria semantics (`frontend/tests/upload-form.test.tsx`, `download-panel.test.tsx`). No vacuous, weakened, or unreachable assertions found in the samples. Red-first/mutation documentation is consistent but not re-derived (F8).

**Bottom line:** no fabrication indicators found; the harness and tests are real and unmocked. The genuine gaps are provenance-shaped: replayed implement events undisclosed in-band (F1), un-retained/unbound browser artifacts (F2), unverified build freshness (F3), and a non-tamper-evident preview record (F4). The six-lens gate (including this review) must close in the ledger before any release verdict is honest (F6).
