# Contribution ledger — atomic PR series

The monolithic V1/V2 deliveries (closed PRs #1 and #2, preserved immutably as
`backup/v1-monolith` and `backup/v2-monolith`) are re-delivered as a sequence of
atomic, independently reviewed PRs. Each row is completed (merge commit filled in)
by the next PR in the series; the final report closes the last row.

Legend — outcome: what the PR makes usable on `main`. reviewer verdict: from the
independent read-only review recorded on the PR before merge.

| # | PR | Title | Outcome | Requirements | Tests (result) | Reviewer verdict | Merge commit | Limitations / deferred |
|---|----|-------|---------|--------------|----------------|------------------|--------------|------------------------|
| 1 | #3 | Repository foundation | Installable `pmpe` package skeleton, CI (format/lint, strict types, security scan, build+import smoke), PR template, contribution rules, architecture boundary and design docs | PRD §repo foundation; docs/technical-requirements.md SYS-setup | no product tests yet (foundation); CI jobs green | APPROVE (2 rounds, 6 scope-purity findings fixed) | 89599f9 | No functional pipeline yet — ingestion arrives in PR 2 |
| 2 | #4 | Specification ingestion | `pmpe validate` loads JSON/YAML MVP specs, enforces the schema, normalizes to typed MvpSpec, rejects malformed input with exit 2 | TRD ingestion requirements; PRD spec-input contract | 18 unit tests (schema + normalizer + packaged-schema sync guard) passed; CLI smoke on golden + malformed fixtures | APPROVE (2 rounds, 3 findings fixed) | 2a41462 | Structural validation only — semantic requirement validation lands in PR 3 |
| 3 | #5 | Requirement validation | `pmpe validate` now runs semantic checks: missing fields/decisions, contradictions, untestable acceptance criteria, activity-only NSM, undeclared dependencies; errors/questions exit 3 | TRD validation requirements; PRD verification-first principle | planted-failure unit tests (validator) passed | APPROVE (first round) | 81452bf | Heuristic pattern-based checks — catch common failure shapes, not all (documented) |
| 4 | #6 | Engineering planning | Validated specs convert to an EngineeringPlan: task IDs, dependencies, topological order, component/data/API plan, complexity estimates | TRD planning requirements | planner unit tests passed | APPROVE (first round; non-blocking test-strength note on FR-coverage union assertion) | 6254e2c | Plan is consumed by later stages; no execution yet |
| 5 | #7 | Architecture decisions | Architecture Pack with generated ADRs (context/decision/consequences/reversibility), risk classification, and product-decision escalation via the policy engine | TRD architecture requirements; PD-escalation principle | policy-engine unit tests + ingest-to-plan-to-architecture integration tests passed | APPROVE (2 rounds; blocking: no positive escalation coverage, missing CI integration step — both fixed) | 062cb9e | Escalations are data — the approval workflow that consumes them lands with the runtime; 5 POL rules are forward-looking registry surface |
| 6 | #8 | Tests-before-code | Test architect generates the acceptance-criteria test suite (requirement-mapped, negative cases) before any implementation exists | TRD tests-first requirement (SYS-07) | generation integration tests passed | APPROVE (first round; dead-helper nit fr_ids_for_entity retained for backup parity) | b312e46 | Red-state execution proof (confirm_red) lands with the runtime that runs suites |
| 7 | #9 | Reference implementation | Deterministic stdlib CRUD generator (storage/auth/API/server) turns the generated red suite green in an isolated workspace with task-scoped git commits | TRD SYS-08/SYS-09; ADR-003 | 10 integration tests incl. fail-closed workspace boundary; red→green by execution | APPROVE (2 rounds; blocking: boundary guard untested — negative test added) | 33d7c30 | Single reference stack by decision; commits local to the run workspace |
| 8 | #10 | Quality and security gates | Typed gate results over generated workspaces: compile, format, lint, unit, integration, security scan (regex-rule secrets/eval/exec/shell/pickle/SQL-format) with honest skip recording | TRD gate requirements; SECURITY.md model | scanner unit tests + gates-over-workspace integration tests passed | APPROVE (2 rounds; blocking: untested workspace-relative exemption + SEC_EXEC — regression-killing tests added) | e56069b | Gates run on demand — the runtime that orders and persists them lands in PR 9 |
| 9 | #11 | Durable workflow runtime | `pmpe run/resume/approve/status`: ordered step engine (ingest→quality gates) with persisted state, atomic writes, idempotent resume, confirm_red rejecting vacuous all-green suites (import-only red is accepted by design in V1 — pre-implementation suites can only fail at import; executed-evidence red classification arrives with the V2 traceability PR), risk-policy escalations and recorded human approvals | TRD runtime requirements; ADR-006 | workflow-state + config unit tests passed; resume semantics tested at state level | APPROVE (2 rounds; claim correction: V1 confirm_red is vacuous-only, import-only red accepted by design; runtime mechanics empirically demonstrated) | 217f7d9 | Pipeline currently ends at quality gates — review/merge (PR 10) and deploy/report (PR 11) extend STEP_ORDER |
| 10 | #12 | Review and merge control | PR record artifact, deterministic reviewer, allow-listed safe fixes with retest, and the MERGE/NO_MERGE gate (requirement coverage + blocking findings + gate results); NO_MERGE completes the run with exit 4 | TRD review/merge requirements; ADR-004 | merge-gate + traceability unit tests, review-and-fix integration tests passed; pmpe run completes through merge with MERGE decision | APPROVE (first round; fail-closed gate + exit-4 NO_MERGE empirically demonstrated; audit-builder inclusion judged load-bearing) | 00f751b | Reviewer is deterministic rule-based (V1); independent multi-reviewer assurance arrives in V2 |
| 11 | #13 | Deployment, audit, V1 completion | Local process deployment with health + user-journey verification, rollback instructions and promotable artifact, telemetry metrics, final report + pmpe report, full e2e suites; V1 tree byte-converged with backup/v1-monolith (plus this ledger and four review-added tests) | TRD deploy/report requirements; ADR-005; CHANGELOG 0.1.0/0.1.1 | full suite incl. e2e passed; capability matrix + failure paths green | APPROVE (first round; byte-convergence, 155-test suite, real deploy + token-leak scan all verified independently) | 077782e | Local-only deployment by design (ADR-005); V2 begins at PR 12 |
| 12 | #14 | ProductDecisionContract (V2 start) | Typed digest-locked contract: approved-only admission, canonical sha256 digest, versioned registry (changed contract = new version, never overwrite), run-scoped immutability lock failing closed on mutation, ProductChangeRequests, `pmpe contract`/`change-request` CLI | PD-01/PD-02/PD-03 (docs/v2-approved-product-decisions.md) | 22 new tests (17 contract unit + 5 contract CLI e2e); 150 passed in scope, 177 collected full-suite | APPROVE (2 rounds; blocking: APPROVED-PCR guard had no removal-failing test — regression test added and mutation-verified) | ff98707 | Contracts admit runs when the run engine lands (PR 18); until then validate/digest/diff/PCR flows are the surface |
| 13 | #15 | Engineering agent plane | Six V2 agent definitions (system architect, implementation planner, engineer router, backend/test/integration engineers) parsed and property-checked by `AgentRegistry`; minimum-routing validation (every task covered, every non-selection justified); runtime read-only tree guard (digest snapshot + violation report); isolated per-specialist git worktrees; append-only evidence ledger; frozen immutable candidates (digest-pinned) | PD-05/PD-06 (docs/v2-approved-product-decisions.md) | 21 new tests (12 agent-plane + 2 evidence-ledger unit; 3 worktree-isolation + 4 candidate-freeze integration); 171 passed in scope, 198 collected | APPROVE (2 rounds; blocking: three definitions present-tense referenced the unshipped `pmpe eng` CLI — reworded; `AgentRegistry.all()` gained a mutation-killing test; all nine enforcement properties mutation-verified) | e2ba689 | Read-only permission proofs for reviewer agents land with the assurance PR (14); submission validators land with their consumers (evals, PR 17) |
| 14 | #16 | Independent assurance reviewers | Four read-only reviewer definitions (code, product-conformance, architecture-simplicity, eval-integrity/drift) and the permission model that proves read-only from tool configuration alone: `is_read_only` (empty tool list = inherit-all = NOT read-only), `assert_reviewers_read_only` failing closed on any write-capable or missing reviewer | PD-06 (docs/v2-approved-product-decisions.md) | 6 new permission-proof tests (planted write tool, inherit-all, missing reviewer — each mutation-verified); 177 passed in scope, 204 collected | APPROVE (2 rounds; blocking: inherit-all rule and missing-reviewer branch survived neutering — mutation-killing tests added and re-verified) | c68720a | Reviewer definitions specify the artifacts they audit; executed-evidence and eval inputs arrive in PRs 16–17 and the engine invokes reviewers in PR 18. Fixer stays out until PR 15 |
| 15 | #17 | Findings and approved fixes | Structured findings lifecycle (`FindingsStore`: reviewer originals preserved verbatim, enforced machine PROPOSED→ACCEPTED/REJECTED/DUPLICATE/PRODUCT_DECISION_REQUIRED→FIXED→VERIFIED, decisions need a named decider + reason, verifier ≠ fixer, same-candidate rule failing closed on digest mismatch); owner reconciliation (`reconcile`: low+mechanical auto-accept by recorded policy, undecided findings reported for the engine to act on, duplicates linked never erased, product-behaviour findings become ProductChangeRequests); the approved-findings fixer definition + `FixerGate` (fixer acts only on ACCEPTED finding IDs and files granted findings name; extra files only by explicit caller grant — PD-07) | PD-07 (docs/v2-approved-product-decisions.md) | 23 new tests (findings lifecycle + reconciliation + fixer gate, incl. planted violations; every guard mutation-verified); 200 passed in scope, 227 collected | APPROVE (2 rounds; blocking: claim/code mismatches reworded to shipped semantics + four surviving guards gained one-to-one mutation-killing tests) | 56ae2d0 | Evidence-field completeness is enforced at the submission boundary (validators, PR 17); failing a run on undecided blocking findings is the engine's job (PR 18); until then the lifecycle stores and gate are the shipped surface |
| 16 | #18 | Executed traceability | Requirements classified by EXECUTED test results, not markers: `run_tests_with_evidence` runs a workspace's unittest suite in a stdlib subprocess under an evidence-collecting result class, recording per-node outcomes with failure kinds (assertion / import / error / skip); `build_executed_traceability` binds contract requirements to executed results — VERIFIED (mapped node ran and passed), FAILED (its own assertion failed), NOT_PROVEN (missing, never executed, skipped, or dead on import — none of which is evidence), BLOCKED_PRODUCT_DECISION (open PCR); `red_summary` classifies a red suite by failure kind so vacuous import-only red is distinguishable from intended assertion red (the honest fix for V1's confirm_red limitation recorded in row 9) | PD-06's Eval Integrity & Drift Auditor charter ("coverage is proven by executed evidence"); docs/v2-implementation-plan.md acceptance rows for test_evidence.py ("executed evidence; import-red and skips distinguishable") and executed.py ("anti-gaming: markers alone never prove coverage") | 16 new tests (13 executed-traceability unit incl. planted import-dead, in-body import failure, and skip cases; 3 test-evidence integration with real subprocess runs over green/red/import-broken workspaces); 216 passed in scope, 243 collected | APPROVE (2 rounds; blocking: requirement citation named PD-08 ("Draft PR only") instead of the real anchors — corrected; executed-node import branch gained a one-to-one mutation-killing test) | 98f154c | Consumed by the engine's traceability stage and the eval-integrity auditor in PRs 17–18; until then the evidence runner and classifier are the shipped surface |
| 17 | #19 | Agent and trajectory reliability (evals) | Per-agent eval registry (`evals/agents/*.yaml` ×11 loaded and run against the real definitions: permission cases from tool config, fire/no-fire routing cases, submission-boundary cases via the validators); submission validators land here with their consumer (`pmpe.engineering.submissions`: architecture/plan/specialist/integration/review/fixer output validation incl. evidence-mandatory findings and PD-06/PD-07 boundary refusals); 14 deterministic trajectory rules (TRAJ-01..14) over evidence ledgers (tests-before-implementation, review-after-freeze, reviewer wrote nothing, no self-review, no unauthorized fix, PCR before product change, approval before prod deploy, …); drift reporter across five categories with NEW-hard-gate-failure = HOLD always; judge calibration agreement report + uncalibrated queue; `pmpe evals run` / `pmpe drift compare` CLI; CI gains the eval suite and a planted-regression drift-HOLD step | PD-06 (eval-integrity/drift auditor charter), PD-05 (routing enforcement); docs/v2-implementation-plan.md evals acceptance row ("planted trajectory/drift fixtures must be caught") | 33 new tests (6 agent-eval + 16 trajectory + 11 drift/calibration) over 14 planted trajectory fixtures + 1 clean good-run and 6 drift fixtures; CI runs the suites via the wheel-installed CLI and pins the planted-regression drift step to exit 3 (HOLD); 249 passed in scope, 276 collected | APPROVE (2 rounds; blocking: PR-body per-file counts corrected to measured 6+16+11 and 14+1 fixtures; CI drift step pinned to exit 3 so a crash cannot satisfy it; all fixtures, six mutations, and real-definition evals verified) | 1070751 | Trajectory rules read evidence ledgers; the engine emits them live as of PR 18. Drift thresholds are provisional defaults, labeled as such |
| 18 | #20 | Release orchestration, V2 completion | The V2 run engine (`EngineeringRun`: contract admission + digest lock → architecture → plan → routing → specialists in isolated worktrees → integration + candidate freeze → four-reviewer assurance intake → reconciliation → authorized fixes → executed traceability → draft-PR handoff, every stage ledgered and resumable); deployment policy ladder with digest-bound production approval + simulated production executor (honestly labeled); `pmpe eng`/`pmpe demo` CLI; synthetic end-to-end demonstration with planted failures that must be caught; `/production-engineer` skill (fixtures committed before SKILL.md per repo discipline); V2 docs, README, CHANGELOG 0.2.0, final ci.yml; regenerated final-verification evidence. Owner-mandated hardening round: dirty worktrees cannot freeze; contract binary release gates enforced + persisted at the release report; test/retest evidence digest-bound to the shipped candidate; integrity verification mandatory on every deployment path; production readiness (rollback, artifact, attested health/journey) precedes authorization | PD-01..PD-12 (docs/v2-approved-product-decisions.md); docs/v2-implementation-plan.md steps 14–19 | 50 backup tests + 12 hardening regression tests (each proven mutation-sensitive); full V1+V2 suite 338 passed; demo with planted failures caught; CI runs validate/run/evals/drift/demo from the wheel | APPROVE (2 rounds reconciling the independent reviewer, the Codex P1 review, and five owner-mandated blocking findings; all eight mutations killed one-to-one; parity delta limited to the four itemized hardening files) | 2d89195 | Production deployment is simulated by decision (PD-09 ladder is policy-real, executor synthetic, labeled); model-invoking agent stages require the Claude Code runtime (PD-11) — the engine enforces artifacts and gates deterministically |
| 19 | #22 | V3: contract, architecture, threat model, plan, schema | Locked V3 product decisions (PD-V3-01..18: full-stack web delivery, pm-evals Web journey and verdict semantics, guardrails, reference stack, dogfood mandate); baseline verification from main (338 green); architecture for the full-stack adapter + product layout; threat model (12 threats with controls); 17-PR implementation plan; FullStackProductContract JSON Schema (documented-validator subset) + approved pm-evals Web example | New goal instruction (V3); docs/v3/approved-product-decisions.md | 7 new schema tests (subset guard mutation-proven at two nesting depths, example validates, 5 planted structural violations rejected); 345 passed full-suite | APPROVE (3 rounds; wording-consistency fixes; one incomplete fix commit caught by tree-level verification and corrected) | e6bfcdf | Typed model + semantic runnability land in V3 PR 2; docs describe targets — delivery status tracked in this ledger |
| 20 | #23 | V3: full-stack adapter foundation | Typed `FullStackProductContract` (journey/screens/states/capabilities/entities/APIs/accessibility/responsive/gates/guardrails/deployment/exclusions) with semantic runnability (APPROVED + named approver + no unresolved blocking questions), canonical digest, structural id-uniqueness; the `FullStackAdapter` protocol (seven capability surfaces); the reference Next.js+FastAPI+Playwright stack declaration (preview-only deployment kinds, no cloud claim); fail-closed contract assessment refusing unsupported deployment kinds, permanent persistence, missing core UI states, and uncovered API methods; fail-closed stack registry; packaged schema + sync guard | PD-V3-13/14 (docs/v3/approved-product-decisions.md); plan PR 2 | 22 new tests (10 contract + 12 stack); 12/12 guard mutations killed incl. two reviewer-added probes; 367 passed full-suite | APPROVE (first round; informational notes only — isinstance/mypy division of labor judged honest; process claim reworded) | 7347367 | UX-journey cross-reference validation is PR 3; engine admission wiring is PR 15; product scaffolding lands with its first real content (PR 4) — empty scaffolding would be theatre |
| 21 | #24 | V3: UX architecture validation | `validate_ux_architecture`: five coherence rules over the contract's journey/screens/states (journey step must point at an existing screen; every screen reachable from the journey; declared states within the vocabulary; every screen declares an error state; no vocabulary state left undeclared) with named, accumulating refusals; `record_validated_journey` persists the screen/state/journey inventory only for coherent contracts (nothing written on refusal); `require_validated_journey` fails closed when no record exists or the record belongs to a different contract digest — the implementation-before-journey precondition later stages enforce (PD-V3-16) | PD-V3-16 (docs/v3/approved-product-decisions.md); plan PR 3 | 12 new tests; 9/9 mutation probes killed incl. two reviewer-added fail-open probes; 379 passed full-suite | APPROVE (first round; informational notes on untyped JSON-decode failure and record trust model — engine wiring deferred to PR 15 as disclosed) | 1a53b6c | Engine-stage wiring (admission refusing implementation artifacts before a validated journey) lands with orchestration (PR 15); TRAJ-FS plants the same failure class in PR 14 |
| 22 | #25 | V3: pm-evals deterministic comparison engine | `pm_evals_compare` (first product code, own package under products/pm-evals-web/backend): strict bounded eval-run format v1 (typed criteria with hard-gate flags and min_pass_rate guardrails, pass/fail trace results, named parse issues — never stack traces); pair compatibility checks; trace-level comparison (overall + per-criterion pass rates, deltas, newly passing/failing traces, hard-gate regressions); locked verdict semantics with documented precedence HOLD > INSUFFICIENT_EVIDENCE > PROCEED and trace-level evidence on every reason; deterministic Markdown/JSON reports with caller-injected timestamps isolated to labeled fields; three realistic synthetic fixtures (baseline/improved/regression); CI gains the product-backend job (pytest + mypy --strict + ruff) | PD-V3-04/05/07 (docs/v3/approved-product-decisions.md); plan PR 4 | 24 new engine tests; 11 guard mutations killed (8 author + 3 reviewer probes); determinism reproduced across hash seeds; pmpe suite untouched (379) | APPROVE (first round; two minor findings resolved pre-merge: baseline-threshold fallback pinned by test + correctly documented; guardrail coverage-by-omission gap disclosed for the hardening round) | 5cff784 | API layer (upload/size limits/report endpoints) is PR 5; the engine is import-only until then. Guardrail thresholds fall back to the baseline's declaration (stricter, fail-closed). DISCLOSED GAP (reviewer F2): coverage requirements apply to hard gates only — a guardrail criterion's rate is computed over the traces that carry results, so omission shrinks the denominator without a reason firing; follow-up decision recorded for the dogfood/hardening round. Product tests run in their own pytest rootdir — the pmpe suite stays at 379 |
| 23 | #26 | V3: pm-evals Web backend API | FastAPI transport over the deterministic engine (contract APIs API-1..3): `POST /api/compare` (multipart baseline+candidate, per-file 5 MB cap enforced before parsing, in-memory only, sha256 input digests, optional min_matched_traces), `POST /api/report` (Markdown/JSON downloads with server-generated filenames — uploaded names never reach headers), `GET /api/health`; malformed files → 422 with named per-source issues (J-4), incompatible-but-parseable pairs → 200 with verdict HOLD (PD-V3-04 binding mapping); committed `openapi.json` is the contract, pinned byte-for-byte against the live app by test; deterministic responses across requests; spool-residue test proves no persisted uploads | PD-V3-04/07/08 (docs/v3/approved-product-decisions.md); contract APIs API-1..3, threats T2/T3/T11; plan PR 5 | 16 new API tests; 5 guard mutations killed; nested-JSON probed to depth 100k by the reviewer | APPROVE (2 rounds; blocking: RecursionError 500 broke the locked 422 mapping — fixed + mutation-verified; digest pin, typed OpenAPI responses, byte-level contract pin) | d552918 | The typed frontend client + CI contract-diff step land in PR 6 (pmpe.fullstack.api_contract); uvicorn serving + preview harness in PR 12 |
| 24 | #27 | V3: pm-evals Web frontend shell + typed API contract | Hand-rolled minimal Next.js 15 + React 19 + TypeScript app (no design-system dependency, PD-V3-13): S-1 shell with the real J-1 explainer (plain-words product explanation, the three verdicts, the never-stored promise); same-origin /api proxying via Next rewrites (no CORS surface, T1); typed API client whose every shape derives from `src/lib/api-types.gen.ts`, GENERATED from the committed backend openapi.json — the UI cannot depend on an undocumented field; client maps the J-4 named-issues 422, framework 422s, 413, and network failure to typed recoverable results; `pmpe.fullstack.api_contract` (promised-API coverage + committed-vs-live drift checks, fail closed); CI gains the product-frontend job (npm ci, high-severity audit gate, typed-client drift check via regenerate+git-diff, tsc, vitest, next build) | PD-V3-13 (docs/v3/approved-product-decisions.md); contract API-1..3; threats T1/T9; plan PR 6 | 13 frontend tests (compareRuns ok/named-422/framework-422/413/network/empty-detail/non-JSON-422/non-JSON-200; downloadReport ok/413/named-422; health; page explainer with all three verdicts) + 8 pmpe api_contract tests (real committed doc covers the product contract; planted missing-path/wrong-method/no-paths/drift/formatting-only-drift/missing-file violations) | APPROVE (2 rounds; round 1 REQUEST_CHANGES: the PR's own audit gate red — next 15.5.12 high advisories incl. request smuggling in rewrites, vitest 2.x chain critical — plus two false body claims, missing downloadReport coverage, an unpinned byte-drift promise, and client edge cases; all fixed in db1230a — next 15.5.20 / vitest 4.1.10, audit exit 0 — and verified fix-by-fix by a second fresh-context reviewer incl. re-running the surviving mutation: killed) | 005e252 | The upload journey (J-2..J-5) is PR 7; dashboard PR 8; the client's report/download path is exercised end-to-end in PR 11. Two moderate advisories remain (postcss pinned inside next), below the committed gate level |
| 25 | #28 | V3: pm-evals Web upload journey | Screen S-1's upload-and-compare journey (J-2..J-5, J-9): labeled baseline/candidate file inputs; client-side pre-validation (`src/lib/validate.ts`) mirroring the backend's parse and compatibility language — 5 MB cap refused on size alone, JSON/object/format_version/suite/criteria/traces checks, pair checks (suite mismatch, no shared criteria, no shared trace ids) — advisory by design (fails open; the server stays authoritative): blocking issues are only the checks the server is KNOWN to refuse, while browser-unparseable files (JSON.parse is stricter than Python's json.loads — NaN tokens, encoding differences) warn advisorily and stay submittable so the server decides; lossy id extraction suppresses shared-id pair checks (the server's field-level 422 names the real problem); the contract's four S-1 states explicit (empty/loading/error/success); file inputs locked while a comparison is in flight AND submission blocked while any file read is pending — together no handleSelect completion can land mid-flight, so a comparison is only ever submitted for the pair the form shows; server-side named issues, 413, and network failure surfaced in a `role="alert"` region; loading and verdict announced via `role="status"` `aria-live="polite"` (PD-V3-09, attribute test-pinned); Start-a-new-comparison reset (J-9); hostile filenames and server-sent messages rendered as text only | PD-V3-03/04/09 (docs/v3/approved-product-decisions.md); journey J-2..J-5/J-9; plan PR 7 | 28 new tests (12 validate + 16 component incl. 2 hostile-string and a gated-FileReader race test); tests committed red before implementation (branch history); 9 mutations killed (size cap, format_version, suite mismatch, canSubmit-ignores-pair, alert role, aria-live, stale-error clearing, advisory-made-blocking, pending-read-submit-block) | APPROVE (3 rounds, 3 distinct fresh-context reviewers; R1: fail-closed parse divergence — client refused NaN/UTF-16 files the server accepts — plus 4 minors; R2: verified all R1 fixes, demonstrated the residual pending-read race R2-1 empirically; R3: pendingReads gate traced through every ordering, mutation killed by exactly the pinning assertion) | 6a395dc | The evidence dashboard (S-2, J-6) is PR 8; downloads (J-8) PR 9; axe + keyboard automation PR 10. Client pre-flight blocking of parseable incompatible pairs means the server's 200-HOLD path is normally reached via advisory submissions; PR 11 exercises it end-to-end |
| 26 | #29 | V3: pm-evals Web comparison dashboard + trace explorer | Screen S-2 (J-6): evidence-backed verdict panel — reason kinds labeled, criterion ids, trace-level evidence lists, engine detail strings verbatim; summary (suite, run ids, matched/one-sided trace counts, pass rates, signed net change, input digests as provenance); criterion-delta table with hard-gate badges and regressed-row marking — every number from the engine payload, display formatting only (PD-V3-05/07). Screen S-3 (J-7): changed-trace list with direction labels, text + direction filters, keyboard-reachable per-trace detail deriving criterion flips from newly_passing/newly_failing, honest empty/no-match states and a defensive error state for evidence-less traces. Workspace wiring: UploadForm onComparison callback (value on success, null on any invalidation) — a stale verdict never stands beside new inputs. Golden fixtures are REAL engine output (PROCEED/HOLD/INSUFFICIENT_EVIDENCE), byte-pinned by a backend test that also requires all three verdicts represented | PD-V3-03/05/07 (docs/v3/approved-product-decisions.md); journey J-6/J-7; plan PR 8 | 19 new tests (6 dashboard + 8 explorer + 3 workspace + 2 backend golden pins); tests committed red before implementation; 7 mutations killed (evidence line dropped, sign lost, filter ignored, reason mislabeled, invalidation not reported upward, regression flips skipped, doctored golden fixture) | APPROVE (round 1; MINOR zero-delta '+0.0%' fixed pre-merge + mutation-pinned; reviewer independently regenerated the goldens byte-identical and killed 6 further mutations incl. a doctored golden; PD-V3-05/07 conformance read line-by-line — only display formatting, evidence line load-bearing) | 1f34ff1 | A11y automation (axe) PR 10; real-backend browser proof PR 11. S-3 loading state does not apply client-side (data already local) — exercised at journey level in PR 11. Reviewer notes recorded: backend scripts/ outside CI lint scope (functionally gated via the pin test); trace-detail panel persists when a filter excludes the selection (UX consideration for PR 10/11) |
| 27 | TBD | V3: pm-evals Web report downloads (J-8) | DownloadPanel: Markdown/JSON report downloads through the real typed client under the server's constant Content-Disposition filenames; object URL created→clicked→revoked (no leaks); one download at a time (both buttons locked whichever format is in flight); in-flight state announced via role=status; failed downloads a recoverable role=alert cleared on retry; named 422 issues and network failures surfaced. UploadForm hands off ComparisonHandoff (comparison + the exact File pair it came from) so the report regenerates server-side from the same evidence with nothing stored (PD-V3-08). Dashboard INSUFFICIENT_EVIDENCE guidance rendered only for that verdict — every S-2 contract state now reachable and tested (success/insufficient-evidence/error-via-download-failure; compare-loading is owned by S-1 as disclosed) | PD-V3-03/08 (docs/v3/approved-product-decisions.md); journey J-8; plan PR 9 | 10 new tests (7 download-panel + 2 workspace + 1 dashboard IE-guidance); tests committed red before implementation; 4 mutations killed (object-URL leak, stale-error-on-retry, per-button-lock weakening — added the inverse-direction pin after the first probe survived, IE-guidance-unconditional) | pending | pending | Trace-detail filter persistence and axe/keyboard automation land in PR 10; real-backend browser journey PR 11 |
