# Contribution ledger — atomic PR series

The monolithic V1/V2 deliveries (closed PRs #1 and #2, preserved immutably as
`backup/v1-monolith` and `backup/v2-monolith`) are re-delivered as a sequence of
atomic, independently reviewed PRs. Each row is completed (merge commit filled in)
by the next PR in the series; the final report closes the last row.

Legend — outcome: what the PR makes usable on `main`. reviewer verdict: from the
independent read-only review recorded on the PR before merge.

| # | PR | Title | Outcome | Requirements | Tests (result) | Reviewer verdict | Merge commit | Limitations / deferred |
|---|----|-------|---------|--------------|----------------|------------------|--------------|------------------------|
| 1 | #3 | Repository foundation | Installable `pmpe` package skeleton, CI (format/lint, strict types, security scan, build+import smoke), PR template, contribution rules, architecture boundary and design docs | PRD §repo foundation; docs/technical-requirements.md SYS-setup | no product tests yet (foundation); CI jobs green | APPROVE (2 rounds, 6 scope-purity findings fixed) | 89599f9 | No functional pipeline yet — ingestion arrives in PR 2 |
| 2 | #4 | Specification ingestion | `pmpe validate` loads JSON/YAML MVP specs, enforces the schema, normalizes to typed MvpSpec, rejects malformed input with exit 2 | TRD ingestion requirements; PRD spec-input contract | 18 unit tests (schema + normalizer + packaged-schema sync guard) passed; CLI smoke on golden + malformed fixtures | APPROVE (2 rounds, 3 findings fixed) | 2a41462 | Structural validation only — semantic requirement validation lands in PR 3 |
| 3 | #5 | Requirement validation | `pmpe validate` now runs semantic checks: missing fields/decisions, contradictions, untestable acceptance criteria, activity-only NSM, undeclared dependencies; errors/questions exit 3 | TRD validation requirements; PRD verification-first principle | planted-failure unit tests (validator) passed | APPROVE (first round) | 81452bf | Heuristic pattern-based checks — catch common failure shapes, not all (documented) |
| 4 | #6 | Engineering planning | Validated specs convert to an EngineeringPlan: task IDs, dependencies, topological order, component/data/API plan, complexity estimates | TRD planning requirements | planner unit tests passed | APPROVE (first round; non-blocking test-strength note on FR-coverage union assertion) | 6254e2c | Plan is consumed by later stages; no execution yet |
| 5 | #7 | Architecture decisions | Architecture Pack with generated ADRs (context/decision/consequences/reversibility), risk classification, and product-decision escalation via the policy engine | TRD architecture requirements; PD-escalation principle | policy-engine unit tests + ingest-to-plan-to-architecture integration tests passed | APPROVE (2 rounds; blocking: no positive escalation coverage, missing CI integration step — both fixed) | 062cb9e | Escalations are data — the approval workflow that consumes them lands with the runtime; 5 POL rules are forward-looking registry surface |
| 6 | #8 | Tests-before-code | Test architect generates the acceptance-criteria test suite (requirement-mapped, negative cases) before any implementation exists | TRD tests-first requirement (SYS-07) | generation integration tests passed | APPROVE (first round; dead-helper nit fr_ids_for_entity retained for backup parity) | b312e46 | Red-state execution proof (confirm_red) lands with the runtime that runs suites |
| 7 | #9 | Reference implementation | Deterministic stdlib CRUD generator (storage/auth/API/server) turns the generated red suite green in an isolated workspace with task-scoped git commits | TRD SYS-08/SYS-09; ADR-003 | 10 integration tests incl. fail-closed workspace boundary; red→green by execution | APPROVE (2 rounds; blocking: boundary guard untested — negative test added) | 33d7c30 | Single reference stack by decision; commits local to the run workspace |
| 8 | #10 | Quality and security gates | Typed gate results over generated workspaces: compile, format, lint, unit, integration, security scan (regex-rule secrets/eval/exec/shell/pickle/SQL-format) with honest skip recording | TRD gate requirements; SECURITY.md model | scanner unit tests + gates-over-workspace integration tests passed | APPROVE (2 rounds; blocking: untested workspace-relative exemption + SEC_EXEC — regression-killing tests added) | e56069b | Gates run on demand — the runtime that orders and persists them lands in PR 9 |
| 9 | #11 | Durable workflow runtime | `pmpe run/resume/approve/status`: ordered step engine (ingest→quality gates) with persisted state, atomic writes, idempotent resume, confirm_red rejecting vacuous all-green suites (import-only red is accepted by design in V1 — pre-implementation suites can only fail at import; executed-evidence red classification arrives with the V2 traceability PR), risk-policy escalations and recorded human approvals | TRD runtime requirements; ADR-006 | workflow-state + config unit tests passed; resume semantics tested at state level | APPROVE (2 rounds; claim correction: V1 confirm_red is vacuous-only, import-only red accepted by design; runtime mechanics empirically demonstrated) | 217f7d9 | Pipeline currently ends at quality gates — review/merge (PR 10) and deploy/report (PR 11) extend STEP_ORDER |
| 10 | #12 | Review and merge control | PR record artifact, deterministic reviewer, allow-listed safe fixes with retest, and the MERGE/NO_MERGE gate (requirement coverage + blocking findings + gate results); NO_MERGE completes the run with exit 4 | TRD review/merge requirements; ADR-004 | merge-gate + traceability unit tests, review-and-fix integration tests passed; pmpe run completes through merge with MERGE decision | APPROVE (first round; fail-closed gate + exit-4 NO_MERGE empirically demonstrated; audit-builder inclusion judged load-bearing) | 00f751b | Reviewer is deterministic rule-based (V1); independent multi-reviewer assurance arrives in V2 |
| 11 | #13 | Deployment, audit, V1 completion | Local process deployment with health + user-journey verification, rollback instructions and promotable artifact, telemetry metrics, final report + pmpe report, full e2e suites; V1 tree byte-converged with backup/v1-monolith (plus this ledger and four review-added tests) | TRD deploy/report requirements; ADR-005; CHANGELOG 0.1.0/0.1.1 | full suite incl. e2e passed; capability matrix + failure paths green | APPROVE (first round; byte-convergence, 155-test suite, real deploy + token-leak scan all verified independently) | 077782e | Local-only deployment by design (ADR-005); V2 begins at PR 12 |
| 12 | #14 | ProductDecisionContract (V2 start) | Typed digest-locked contract: approved-only admission, canonical sha256 digest, versioned registry (changed contract = new version, never overwrite), run-scoped immutability lock failing closed on mutation, ProductChangeRequests, `pmpe contract`/`change-request` CLI | PD-01/PD-02/PD-03 (docs/v2-approved-product-decisions.md) | 22 new tests (17 contract unit + 5 contract CLI e2e); 150 passed in scope, 177 collected full-suite | APPROVE (2 rounds; blocking: APPROVED-PCR guard had no removal-failing test — regression test added and mutation-verified) | ff98707 | Contracts admit runs when the run engine lands (PR 18); until then validate/digest/diff/PCR flows are the surface |
| 13 | #15 | Engineering agent plane | Six V2 agent definitions (system architect, implementation planner, engineer router, backend/test/integration engineers) parsed and property-checked by `AgentRegistry`; minimum-routing validation (every task covered, every non-selection justified); runtime read-only tree guard (digest snapshot + violation report); isolated per-specialist git worktrees; append-only evidence ledger; frozen immutable candidates (digest-pinned) | PD-05/PD-06 (docs/v2-approved-product-decisions.md) | 21 new tests (12 agent-plane + 2 evidence-ledger unit; 3 worktree-isolation + 4 candidate-freeze integration); 171 passed in scope, 198 collected | APPROVE (2 rounds; blocking: three definitions present-tense referenced the unshipped `pmpe eng` CLI — reworded; `AgentRegistry.all()` gained a mutation-killing test; all nine enforcement properties mutation-verified) | e2ba689 | Read-only permission proofs for reviewer agents land with the assurance PR (14); submission validators land with their consumers (evals, PR 17) |
| 14 | #16 | Independent assurance reviewers | Four read-only reviewer definitions (code, product-conformance, architecture-simplicity, eval-integrity/drift) and the permission model that proves read-only from tool configuration alone: `is_read_only` (empty tool list = inherit-all = NOT read-only), `assert_reviewers_read_only` failing closed on any write-capable or missing reviewer | PD-06 (docs/v2-approved-product-decisions.md) | 6 new permission-proof tests (planted write tool, inherit-all, missing reviewer — each mutation-verified); 177 passed in scope, 204 collected | APPROVE (2 rounds; blocking: inherit-all rule and missing-reviewer branch survived neutering — mutation-killing tests added and re-verified) | c68720a | Reviewer definitions specify the artifacts they audit; executed-evidence and eval inputs arrive in PRs 16–17 and the engine invokes reviewers in PR 18. Fixer stays out until PR 15 |
| 15 | TBD | Findings and approved fixes | Structured findings lifecycle (`FindingsStore`: evidence-mandatory findings, OPEN→ACCEPTED/REJECTED/ESCALATED→FIXED→VERIFIED transitions enforced, same-candidate rule failing closed on digest mismatch); owner reconciliation policy (`reconcile`: blocking findings decided, product-behaviour findings become ProductChangeRequests); the approved-findings fixer definition + `FixerGate` allowlist (fixer touches only files granted by ACCEPTED findings — PD-07) | PD-07 (docs/v2-approved-product-decisions.md) | findings-lifecycle + reconciliation + fixer-gate unit tests incl. planted violations | pending | pending | The engine wires reviewers → findings → reconcile → fixer in PR 18; until then the lifecycle stores and gate are the shipped surface |
